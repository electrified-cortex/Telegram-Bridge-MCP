Chat — Chat metadata and information.

Routes:
- chat/info — get chat id, type, title, username, description (requires operator approval)

action(type: "chat") — lists sub-paths in live API.

Each call requires operator Allow/Deny interaction — use sparingly.
Primarily for initial setup, diagnostics, or context recovery.

Related: session/list, session/start