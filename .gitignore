node_modules/
dist/
coverage/
.env
*.js.map
node_modules
temp/
.temp/
data/
# Runtime log files
*.log
# Session state files — regenerated each session by the agent
HANDOFF.md
memory.md
# Legacy session state file (superseded by data/session-snapshot.json)
session-state.json
# Local operator config — copy mcp-config.example.json to customize
mcp-config.json
# Git worktrees — managed locally, not tracked in source
.worktrees/
# Local scratchpad: dispatch briefings, PR body drafts, audit dumps
notes/
# Code review eval artefacts — local-only (contain absolute paths)
.code-reviews/

# Provisioned foreman pods — all variants (standard + temp suffixes)
.foreman-pod*/
# Claude --bg session tracking artifact (written by spawn.sh when pod cwd is here)
.session-id

.scratch/

# Session token — live credential, never committed
memory/telegram/session.token